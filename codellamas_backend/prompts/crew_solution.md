You will generate the correct solution/refactor.

Problem JSON:
{problem_json}

Tests JSON:
{tests_json}

Output ONLY one JSON object:

{
  "solution": {
    "src/main/java/.../X.java": "file content",
    "...": "..."
  }
}

Rules:
- The solution MUST pass the tests.
- The refactor MUST address the selected smells.
Seed: {seed}
